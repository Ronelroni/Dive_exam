<p id="notice"><%= notice %></p>

<h1>Railways</h1>

<table>
  <thead>
    <tr>
      <th>Name of railway line</th>
      <th>Station name</th>
      <th>How many minutes walk</th>
      <th>Property</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @railways.each do |railway| %>
      <tr>
        <td><%= railway.name_of_railway_line %></td>
        <td><%= railway.station_name %></td>
        <td><%= railway.how_many_minutes_walk %></td>
        <td><%= railway.property %></td>
        <td><%= link_to 'Show', railway %></td>
        <td><%= link_to 'Edit', edit_railway_path(railway) %></td>
        <td><%= link_to 'Destroy', railway, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Railway', new_railway_path %>
